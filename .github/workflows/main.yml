name: Main

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  main:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v1

      - uses: actions/cache@v2
        with:
          path: .build
          key: ${{ runner.os }}-spm-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-spm-

      - name: Install xcpretty
        run: gem install xcpretty

      - name: SwiftFormat
        run: swift run -c release swiftformat ../ --lint
        working-directory: ./BuildTools

      - name: Build & Test
        run: |
          set -o pipefail && \
          xcodebuild \
            -scheme "Logto SDK-Package" \
            -destination "platform=iOS Simulator,OS=15.2,name=iPhone 13 Pro" \
            -enableCodeCoverage=YES \
            -derivedDataPath ./DerivedData \
            test | \
          xcpretty

      - name: Convert Swift Test Coverage report
        uses: sersoft-gmbh/swift-coverage-action@v2
        id: coverage-files
        with:
          search-paths: ./DerivedData
          output: ./.coverage
          format: lcov

      - name: Codecov Report Upload
        uses: codecov/codecov-action@v2
        with:
          flags: swift-sdk
          files: ${{join(fromJSON(steps.coverage-files.outputs.files), ',')}}
